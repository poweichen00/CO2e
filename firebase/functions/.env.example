# Copy to functions/.env for local emulation. NEVER commit the real file.
# In production, prefer Secret Manager:
#   firebase functions:secrets:set OPENAI_API_KEY
OPENAI_API_KEY=sk-your-openai-key-here
